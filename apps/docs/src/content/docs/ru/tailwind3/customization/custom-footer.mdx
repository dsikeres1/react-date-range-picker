---
sidebar_position: 4
sidebar_label: Custom Footer
---

import CodeDemo from "@/components/CodeDemo.astro";
import { ExampleWrapper } from "@/components/ExampleWrapper";

import CompoundCustomFooter from "@examples/tailwind3/Customization/CompoundCustomFooter";
import CompoundCustomFooterSource from "@examples/tailwind3/Customization/CompoundCustomFooter.tsx?raw";
import CompoundNoFooter from "@examples/tailwind3/Customization/CompoundNoFooter";
import CompoundNoFooterSource from "@examples/tailwind3/Customization/CompoundNoFooter.tsx?raw";

# Пользовательский футер

## Перегруппированный футер

Переставляйте кнопки в футере и добавляйте дополнительные действия, например, кнопку "Сегодня". С помощью составных компонентов вы точно контролируете, какие кнопки и в каком порядке отображаются.

<CodeDemo source={CompoundCustomFooterSource}>
  <ExampleWrapper variant="tailwind3" client:visible>
    <CompoundCustomFooter client:visible />
  </ExampleWrapper>
</CodeDemo>

## Минималистичный макет

Удалите любую ненужную часть. Здесь футер полностью опущен для создания минималистичного макета только с календарем:

<CodeDemo source={CompoundNoFooterSource}>
  <ExampleWrapper variant="tailwind3" client:visible>
    <CompoundNoFooter client:visible />
  </ExampleWrapper>
</CodeDemo>

## Переопределение классов

Каждая составная часть принимает проп `className`. Пакет Tailwind v3 использует `cn()` (утилиту, объединяющую `clsx` и `twMerge`) для слияния ваших классов с классами по умолчанию, поэтому конфликтующие утилиты разрешаются корректно:

```tsx
import { DatePicker } from "react-date-range-picker-tailwind3";

<DatePicker.Root value={value} onChange={setValue}>
  <DatePicker.Trigger className="min-w-[300px] px-4 py-3" />
  <DatePicker.Content className="rounded-2xl shadow-xl">
    <DatePicker.Header>
      <DatePicker.PrevButton />
      <DatePicker.Title className="text-lg" />
      <DatePicker.NextButton />
    </DatePicker.Header>
    <DatePicker.Grid />
  </DatePicker.Content>
</DatePicker.Root>;
```
