---
sidebar_position: 3
sidebar_label: Custom Day Cell
---

import CodeDemo from "@/components/CodeDemo.astro";
import { ExampleWrapper } from "@/components/ExampleWrapper";

import CompoundCustomDay from "@examples/tailwind3/Customization/CompoundCustomDay";
import CompoundCustomDaySource from "@examples/tailwind3/Customization/CompoundCustomDay.tsx?raw";

# 自訂日期儲存格

使用 `Day` 組件的 render prop 來客製化每個日期儲存格。這對於新增事件指示器、根據日期屬性自訂樣式，或完全取代日期渲染非常有用。

<CodeDemo source={CompoundCustomDaySource}>
  <ExampleWrapper variant="tailwind3" client:visible>
    <CompoundCustomDay client:visible />
  </ExampleWrapper>
</CodeDemo>

## Day Render Props

| Prop             | Type         | 說明                       |
| ---------------- | ------------ | -------------------------- |
| `date`           | `Date`       | 日期物件                   |
| `day`            | `number`     | 月份中的第幾天 (1-31)      |
| `isToday`        | `boolean`    | 是否為今天                 |
| `isSelected`     | `boolean`    | 此日期是否被選中           |
| `isDisabled`     | `boolean`    | 此日期是否被禁用           |
| `isInRange`      | `boolean`    | 此日期是否在選定的範圍內   |
| `isRangeStart`   | `boolean`    | 是否為範圍的起始點         |
| `isRangeEnd`     | `boolean`    | 是否為範圍的結束點         |
| `isInHoverRange` | `boolean`    | 此日期是否在懸停預覽範圍內 |
| `isHoverTarget`  | `boolean`    | 是否為懸停的終點           |
| `isOutsideDay`   | `boolean`    | 此日期是否屬於相鄰的月份   |
| `isHighlighted`  | `boolean`    | 此日期是否有高亮點         |
| `isFocused`      | `boolean`    | 此日期是否具有鍵盤焦點     |
| `onClick`        | `() => void` | 點擊處理常式               |
| `onMouseEnter`   | `() => void` | 滑鼠移入處理常式           |
| `onMouseLeave`   | `() => void` | 滑鼠移出處理常式           |
