---
sidebar_position: 4
sidebar_label: Behavior
---

import CodeDemo from "@/components/CodeDemo.astro";
import Admonition from "@/components/Admonition.astro";
import { ExampleWrapper } from "@/components/ExampleWrapper";

import ShouldCloseOnSelect from "@examples/styled/DateRangeTimePicker/ShouldCloseOnSelect";
import ShouldCloseOnSelectSource from "@examples/styled/DateRangeTimePicker/ShouldCloseOnSelect.tsx?raw";
import MultiMonth from "@examples/styled/DateRangeTimePicker/MultiMonth";
import MultiMonthSource from "@examples/styled/DateRangeTimePicker/MultiMonth.tsx?raw";
import FormIntegration from "@examples/styled/DateRangeTimePicker/FormIntegration";
import FormIntegrationSource from "@examples/styled/DateRangeTimePicker/FormIntegration.tsx?raw";

# Verhalten

## Bei Auswahl schließen

Wenn `shouldCloseOnSelect` aktiviert ist, bestätigt und schließt der Picker automatisch, wenn eine Voreinstellung angeklickt wird.

<Admonition type="info">
  Bei Pickern mit Zeitauswahl gilt `shouldCloseOnSelect` nur für Klicks auf Voreinstellungen – nicht
  für Klicks auf Daten, da der Benutzer die Zeit noch einstellen muss.
</Admonition>

<CodeDemo source={ShouldCloseOnSelectSource}>
  <ExampleWrapper variant="styled" client:visible>
    <ShouldCloseOnSelect client:visible />
  </ExampleWrapper>
</CodeDemo>

## Mehrere Monate

Zeigen Sie mehr als die standardmäßigen zwei Monate nebeneinander an.

<CodeDemo source={MultiMonthSource}>
  <ExampleWrapper variant="styled" client:visible>
    <MultiMonth client:visible />
  </ExampleWrapper>
</CodeDemo>

## Formularintegration

Verwenden Sie die `name`-Prop, um versteckte Eingabefelder für die native Formularübermittlung einzuschließen.

<Admonition type="tip">
  Das Styling des Senden-Buttons in diesem Beispiel ist benutzerdefiniert – die Bibliothek stellt
  nur den Date-Picker zur Verfügung.
</Admonition>

<CodeDemo source={FormIntegrationSource}>
  <ExampleWrapper variant="styled" client:visible>
    <FormIntegration client:visible />
  </ExampleWrapper>
</CodeDemo>
