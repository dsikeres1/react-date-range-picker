---
sidebar_position: 1
sidebar_label: Getting Started
---

import CodeDemo from "@/components/CodeDemo.astro";
import PackageInstall from "@/components/PackageInstall.astro";
import { ExampleWrapper } from "@/components/ExampleWrapper";

import Tailwind4Basic from "@examples/tailwind4/DatePicker/Basic";
import Tailwind4BasicSource from "@examples/tailwind4/DatePicker/Basic.tsx?raw";

# เริ่มต้นใช้งาน Tailwind CSS v4

แพ็คเกจ Tailwind v4 มีคอมโพเนนต์ตัวเลือกวันที่ (date picker) ที่จัดรูปแบบด้วยคลาสยูทิลิตี้ของ Tailwind CSS v4 และโทเค็นการออกแบบเชิงความหมาย (semantic design tokens)

## การติดตั้ง

<PackageInstall pkg="react-date-range-picker-tailwind4" />

ไม่จำเป็นต้องนำเข้า CSS เพิ่มเติม คอมโพเนนต์ใช้คลาสยูทิลิตี้ของ Tailwind โดยตรง

## Tailwind Preflight และ Reset

แพ็คเกจนี้ถือว่าโปรเจกต์ของคุณเปิดใช้งาน Tailwind CSS Preflight (ซึ่งเป็นค่าเริ่มต้น)
หากสภาพแวดล้อมของคุณปิดใช้งาน Preflight (เช่น Docusaurus, Micro Frontends บางตัว) สไตล์ของปุ่มอาจเสียหาย
ในกรณีนั้น ให้นำเข้าไฟล์ `reset.css` ที่ให้มาเพื่อเริ่มต้นสไตล์ของปุ่มด้วยตนเอง:

```tsx
import "react-date-range-picker-tailwind4/reset.css";
```

## โทเค็นเชิงความหมาย (Semantic Tokens)

คอมโพเนนต์ใช้ตัวแปร CSS เชิงความหมายสำหรับการทำธีม กำหนดค่าได้ในไฟล์ CSS ของคุณ:

```css
@theme {
  --color-popover: oklch(1 0 0);
  --color-popover-foreground: oklch(0.145 0.004 285.823);
  --color-primary: oklch(0.205 0.006 285.885);
  --color-primary-foreground: oklch(0.985 0.001 285.823);
  --color-muted: oklch(0.96 0.003 285.823);
  --color-muted-foreground: oklch(0.556 0.01 285.823);
  --color-accent: oklch(0.96 0.003 285.823);
  --color-accent-foreground: oklch(0.205 0.006 285.885);
  --color-border: oklch(0.922 0.004 285.823);
  --color-input: oklch(0.922 0.004 285.823);
  --color-ring: oklch(0.87 0.006 285.823);
}
```

โทเค็นเหล่านี้จะจับคู่กับคลาสต่างๆ เช่น `bg-popover`, `text-foreground`, `border-border` เป็นต้น

## เริ่มต้นอย่างรวดเร็ว

<CodeDemo source={Tailwind4BasicSource}>
  <ExampleWrapper variant="tailwind4" client:load>
    <Tailwind4Basic client:load />
  </ExampleWrapper>
</CodeDemo>

## มีอะไรบ้าง

| คอมโพเนนต์            | คำอธิบาย                                                 |
| --------------------- | -------------------------------------------------------- |
| `DatePicker`          | การเลือกวันที่เดียวพร้อมปฏิทินป๊อปอัป                    |
| `DateRangePicker`     | การเลือกช่วงวันที่พร้อมปฏิทินคู่และค่าที่ตั้งไว้ล่วงหน้า |
| `DateTimePicker`      | การเลือกวันที่และเวลาพร้อมแผงเลื่อนเวลา                  |
| `DateRangeTimePicker` | การเลือกช่วงวันที่และเวลา                                |

คอมโพเนนต์ทั้งหมดรองรับ:

- ควบคุมค่าด้วย `value` / `onChange`
- การนำทางด้วยคีย์บอร์ด
- โหมดมืดผ่าน `dark:` variant
- 4 ขนาด: `small`, `medium`, `large`, `x-large`
- Compound Component API สำหรับการปรับแต่ง

## ประเภทของ TypeScript

ประเภทการกำหนดค่าและข้อมูลทั้งหมดถูกส่งออกซ้ำจากแพ็คเกจ — ไม่จำเป็นต้องติดตั้งแพ็คเกจ headless แยกต่างหาก:

```tsx
import type {
  Locale,
  DatePickerSize,
  TimeConfig,
  MinuteStep,
  SecondStep,
  TimePrecision,
  HourFormat,
  TimePeriod,
  CaptionLayout,
  WeekDay,
  DateRangePreset,
  CalendarMonth,
  DayProps,
} from "react-date-range-picker-tailwind4";
```

ประเภทเหล่านี้มีประโยชน์สำหรับ:

- **`Locale`** — การกำหนดค่าภาษาท้องถิ่นแบบกำหนดเอง (ชื่อวันในสัปดาห์, ป้ายกำกับปุ่ม, รูปแบบวันที่)
- **`DatePickerSize`** — การกำหนดประเภทของ `size` prop (`"small"` | `"medium"` | `"large"` | `"x-large"`)
- **`TimeConfig`** — การกำหนดค่าความแม่นยำ, รูปแบบ, และขั้นของตัวเลือกเวลา
- **`DateRangePreset`** — การกำหนดค่าที่ตั้งไว้ล่วงหน้าสำหรับ `DateRangePicker` / `DateRangeTimePicker`
- **`CalendarMonth`**, **`DayProps`** — การเรนเดอร์แบบกำหนดเองด้วย Compound Components

ดู [Headless Types](/docs/headless/types/common-types) สำหรับคำจำกัดความของประเภทโดยละเอียด

## ขั้นตอนถัดไป

- [DatePicker](/docs/tailwind4/components/date-picker/basic) — การเลือกวันที่เดียว
- [DateRangePicker](/docs/tailwind4/components/date-range-picker/basic) — ช่วงวันที่พร้อมค่าที่ตั้งไว้ล่วงหน้า
- [Compound Components](/docs/tailwind4/customization/overview) — ปรับแต่งโครงสร้างภายใน
- [Semantic Tokens](/docs/tailwind4/customization/semantic-tokens) — การทำธีมด้วยโทเค็นการออกแบบ
- [Dark Mode](/docs/tailwind4/customization/dark-mode) — การรองรับธีมมืด
