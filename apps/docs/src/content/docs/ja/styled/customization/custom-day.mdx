---
sidebar_position: 3
sidebar_label: "Custom Day Cell"
---

import CodeDemo from "@/components/CodeDemo.astro";
import { ExampleWrapper } from "@/components/ExampleWrapper";

import CompoundCustomDay from "@examples/styled/Customization/CompoundCustomDay";
import CompoundCustomDaySource from "@examples/styled/Customization/CompoundCustomDay.tsx?raw";

# カスタムデイセル

`Day`コンポーネントのレンダープロップを使用して、各日付セルの外観をカスタマイズします。これは、イベントインジケーターやツールチップの追加、または個々の日付に完全にカスタムのスタイリングを適用するのに便利です。

<CodeDemo source={CompoundCustomDaySource}>
  <ExampleWrapper variant="styled" client:load>
    <CompoundCustomDay client:load />
  </ExampleWrapper>
</CodeDemo>

## Dayレンダープロップ

| Prop             | Type         | 説明                                         |
| ---------------- | ------------ | -------------------------------------------- |
| `date`           | `Date`       | 日付オブジェクト                             |
| `day`            | `number`     | 月の日 (1-31)                                |
| `isToday`        | `boolean`    | 今日の日付かどうか                           |
| `isSelected`     | `boolean`    | この日が選択されているかどうか               |
| `isDisabled`     | `boolean`    | この日が無効になっているかどうか             |
| `isInRange`      | `boolean`    | この日が選択された範囲内にあるかどうか       |
| `isRangeStart`   | `boolean`    | 範囲の開始日かどうか                         |
| `isRangeEnd`     | `boolean`    | 範囲の終了日かどうか                         |
| `isInHoverRange` | `boolean`    | この日がホバープレビュー範囲内にあるかどうか |
| `isHoverTarget`  | `boolean`    | ホバーの終点かどうか                         |
| `isOutsideDay`   | `boolean`    | この日が隣接する月に属しているかどうか       |
| `isHighlighted`  | `boolean`    | この日にハイライトドットがあるかどうか       |
| `isFocused`      | `boolean`    | この日にキーボードフォーカスがあるかどうか   |
| `onClick`        | `() => void` | クリックハンドラ                             |
| `onMouseEnter`   | `() => void` | マウスエンターハンドラ                       |
| `onMouseLeave`   | `() => void` | マウスリーブハンドラ                         |
