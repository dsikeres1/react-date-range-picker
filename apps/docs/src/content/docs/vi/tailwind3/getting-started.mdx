---
sidebar_position: 1
sidebar_label: Getting Started
---

import CodeDemo from "@/components/CodeDemo.astro";
import PackageInstall from "@/components/PackageInstall.astro";
import { ExampleWrapper } from "@/components/ExampleWrapper";

import Tailwind3Basic from "@examples/tailwind3/DatePicker/Basic";
import Tailwind3BasicSource from "@examples/tailwind3/DatePicker/Basic.tsx?raw";

# Bắt đầu với Tailwind CSS v3

Gói Tailwind v3 cung cấp các thành phần bộ chọn ngày được tạo kiểu bằng các lớp tiện ích của Tailwind CSS v3.

## Cài đặt

<PackageInstall pkg="react-date-range-picker-tailwind3" />

## Cấu hình Tailwind

Thêm plugin và đường dẫn content vào cấu hình Tailwind của bạn:

```js
// tailwind.config.js
import { rdrpPlugin } from "react-date-range-picker-tailwind3/plugin";

export default {
  content: [
    "./src/**/*.{js,ts,jsx,tsx}",
    "./node_modules/react-date-range-picker-tailwind3/dist/**/*.{js,mjs}",
  ],
  darkMode: "class",
  plugins: [rdrpPlugin],
};
```

## Bắt đầu nhanh

<CodeDemo source={Tailwind3BasicSource}>
  <ExampleWrapper variant="tailwind3" client:load>
    <Tailwind3Basic client:load />
  </ExampleWrapper>
</CodeDemo>

## Bao gồm những gì

| Thành phần            | Mô tả                                              |
| --------------------- | -------------------------------------------------- |
| `DatePicker`          | Chọn một ngày với lịch bật lên                     |
| `DateRangePicker`     | Chọn khoảng ngày với lịch kép và các cài đặt trước |
| `DateTimePicker`      | Chọn ngày + giờ với bảng cuộn thời gian            |
| `DateRangeTimePicker` | Chọn khoảng ngày + giờ                             |

Tất cả các thành phần đều hỗ trợ:

- Giá trị được kiểm soát với `value` / `onChange`
- Điều hướng bằng bàn phím
- Chế độ tối thông qua biến thể `dark:`
- 4 kích thước: `small`, `medium`, `large`, `x-large`
- API Thành phần Hợp chất để tùy chỉnh

## Các loại TypeScript

Tất cả các loại cấu hình và dữ liệu đều được tái xuất từ gói — không cần cài đặt riêng gói headless:

```tsx
import type {
  Locale,
  DatePickerSize,
  TimeConfig,
  MinuteStep,
  SecondStep,
  TimePrecision,
  HourFormat,
  TimePeriod,
  CaptionLayout,
  WeekDay,
  DateRangePreset,
  CalendarMonth,
  DayProps,
} from "react-date-range-picker-tailwind3";
```

Các loại này hữu ích cho:

- **`Locale`** — Ghi đè bản địa hóa tùy chỉnh (tên các ngày trong tuần, nhãn nút, định dạng ngày)
- **`DatePickerSize`** — Định kiểu cho prop `size` (`"small"` | `"medium"` | `"large"` | `"x-large"`)
- **`TimeConfig`** — Cấu hình độ chính xác, định dạng và các bước của bộ chọn thời gian
- **`DateRangePreset`** — Xác định các cài đặt trước cho `DateRangePicker` / `DateRangeTimePicker`
- **`CalendarMonth`**, **`DayProps`** — Kết xuất tùy chỉnh với các Thành phần Hợp chất

Xem [Các loại Headless](/docs/headless/types/common-types) để biết định nghĩa chi tiết về các loại.

## Các bước tiếp theo

- [DatePicker](/docs/tailwind3/components/date-picker/basic) — Chọn một ngày
- [DateRangePicker](/docs/tailwind3/components/date-range-picker/basic) — Khoảng ngày với các cài đặt trước
- [Compound Components](/docs/tailwind3/customization/overview) — Tùy chỉnh cấu trúc bên trong
- [Theme Override](/docs/tailwind3/customization/theme-override) — Tùy chỉnh màu sắc chủ đề
- [Dark Mode](/docs/tailwind3/customization/dark-mode) — Hỗ trợ chủ đề tối
