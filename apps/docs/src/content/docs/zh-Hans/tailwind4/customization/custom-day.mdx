---
sidebar_position: 3
sidebar_label: Custom Day Cell
---

import CodeDemo from "@/components/CodeDemo.astro";
import { ExampleWrapper } from "@/components/ExampleWrapper";

import CompoundCustomDay from "@examples/tailwind4/Customization/CompoundCustomDay";
import CompoundCustomDaySource from "@examples/tailwind4/Customization/CompoundCustomDay.tsx?raw";

# 自定义日期单元格

使用 `Grid` 和 `Day` 渲染属性来自定义每个日期的渲染方式。这使您可以向单个日期单元格添加事件指示器、自定义样式或任何额外内容。

<CodeDemo source={CompoundCustomDaySource}>
  <ExampleWrapper variant="tailwind4" client:load>
    <CompoundCustomDay client:load />
  </ExampleWrapper>
</CodeDemo>

## Day 渲染属性

| Prop             | Type         | 描述                       |
| ---------------- | ------------ | -------------------------- |
| `date`           | `Date`       | 日期对象                   |
| `day`            | `number`     | 月份中的第几天 (1-31)      |
| `isToday`        | `boolean`    | 是否是今天                 |
| `isSelected`     | `boolean`    | 此日期是否被选中           |
| `isDisabled`     | `boolean`    | 此日期是否被禁用           |
| `isInRange`      | `boolean`    | 此日期是否在选定范围内     |
| `isRangeStart`   | `boolean`    | 是否是范围的起始点         |
| `isRangeEnd`     | `boolean`    | 是否是范围的结束点         |
| `isInHoverRange` | `boolean`    | 此日期是否在悬停预览范围内 |
| `isHoverTarget`  | `boolean`    | 是否是悬停的终点           |
| `isOutsideDay`   | `boolean`    | 此日期是否属于相邻月份     |
| `isHighlighted`  | `boolean`    | 此日期是否有一个高亮圆点   |
| `isFocused`      | `boolean`    | 此日期是否具有键盘焦点     |
| `onClick`        | `() => void` | 点击事件处理程序           |
| `onMouseEnter`   | `() => void` | 鼠标进入事件处理程序       |
| `onMouseLeave`   | `() => void` | 鼠标离开事件处理程序       |
