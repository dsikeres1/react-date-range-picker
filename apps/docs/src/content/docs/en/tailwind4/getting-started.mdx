---
sidebar_position: 1
sidebar_label: Getting Started
---

import CodeDemo from "@/components/CodeDemo.astro";
import PackageInstall from "@/components/PackageInstall.astro";
import { ExampleWrapper } from "@/components/ExampleWrapper";

import Tailwind4Basic from "@examples/tailwind4/DatePicker/Basic";
import Tailwind4BasicSource from "@examples/tailwind4/DatePicker/Basic.tsx?raw";

# Getting Started with Tailwind CSS v4

The Tailwind v4 package provides date picker components styled with Tailwind CSS v4 utility classes and semantic design tokens.

## Installation

<PackageInstall pkg="react-date-range-picker-tailwind4" />

Import the default theme tokens (skip this if your project already defines them, e.g. shadcn/ui):

```css
@import "react-date-range-picker-tailwind4/rdrp-theme.css";
```

## Tailwind Preflight & Reset

This package assumes your project has Tailwind CSS Preflight enabled (which is the default).
If your environment disables Preflight (e.g., Docusaurus, some Micro Frontends), the button styles may break.
In that case, import the provided `rdrp-reset.css` to manually initialize the button styles:

```tsx
import "react-date-range-picker-tailwind4/rdrp-reset.css";
```

## Semantic Tokens

The components use semantic CSS variables for theming. Define them in your CSS:

```css
@theme {
  --color-popover: oklch(1 0 0);
  --color-popover-foreground: oklch(0.145 0.004 285.823);
  --color-primary: oklch(0.205 0.006 285.885);
  --color-primary-foreground: oklch(0.985 0.001 285.823);
  --color-muted: oklch(0.96 0.003 285.823);
  --color-muted-foreground: oklch(0.556 0.01 285.823);
  --color-accent: oklch(0.96 0.003 285.823);
  --color-accent-foreground: oklch(0.205 0.006 285.885);
  --color-border: oklch(0.922 0.004 285.823);
  --color-input: oklch(0.922 0.004 285.823);
  --color-ring: oklch(0.87 0.006 285.823);
}
```

These tokens map to classes like `bg-popover`, `text-foreground`, `border-border`, etc.

## Quick Start

<CodeDemo source={Tailwind4BasicSource}>
  <ExampleWrapper variant="tailwind4" client:load>
    <Tailwind4Basic client:load />
  </ExampleWrapper>
</CodeDemo>

## What's Included

| Component             | Description                                         |
| --------------------- | --------------------------------------------------- |
| `DatePicker`          | Single date selection with popup calendar           |
| `DateRangePicker`     | Date range selection with dual calendar and presets |
| `DateTimePicker`      | Date + time selection with time scroll panel        |
| `DateRangeTimePicker` | Date range + time selection                         |

All components support:

- Controlled value with `value` / `onChange`
- Keyboard navigation
- Dark mode via `dark:` variant
- 4 sizes: `small`, `medium`, `large`, `x-large`
- Compound Component API for customization

## TypeScript Types

All configuration and data types are re-exported from the package — no need to install the headless package separately:

```tsx
import type {
  Locale,
  DatePickerSize,
  TimeConfig,
  MinuteStep,
  SecondStep,
  TimePrecision,
  HourFormat,
  TimePeriod,
  CaptionLayout,
  WeekDay,
  DateRangePreset,
  CalendarMonth,
  DayProps,
} from "react-date-range-picker-tailwind4";
```

These types are useful for:

- **`Locale`** — Custom locale overrides (weekday names, button labels, date formatting)
- **`DatePickerSize`** — Typing the `size` prop (`"small"` | `"medium"` | `"large"` | `"x-large"`)
- **`TimeConfig`** — Configuring time picker precision, format, and steps
- **`DateRangePreset`** — Defining presets for `DateRangePicker` / `DateRangeTimePicker`
- **`CalendarMonth`**, **`DayProps`** — Custom rendering with Compound Components

See [Headless Types](/docs/headless/types/common-types) for detailed type definitions.

## Next Steps

- [DatePicker](/docs/tailwind4/components/date-picker/basic) — Single date selection
- [DateRangePicker](/docs/tailwind4/components/date-range-picker/basic) — Date range with presets
- [Compound Components](/docs/tailwind4/customization/overview) — Customize internal structure
- [Semantic Tokens](/docs/tailwind4/customization/semantic-tokens) — Theme with design tokens
- [Dark Mode](/docs/tailwind4/customization/dark-mode) — Dark theme support
