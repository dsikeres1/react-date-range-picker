---
sidebar_position: 2
sidebar_label: Custom Trigger
---

import CodeDemo from "@/components/CodeDemo.astro";
import { ExampleWrapper } from "@/components/ExampleWrapper";

import CompoundCustomTrigger from "@examples/tailwind4/Customization/CompoundCustomTrigger";
import CompoundCustomTriggerSource from "@examples/tailwind4/Customization/CompoundCustomTrigger.tsx?raw";
import CompoundCustomTriggerRange from "@examples/tailwind4/Customization/CompoundCustomTriggerRange";
import CompoundCustomTriggerRangeSource from "@examples/tailwind4/Customization/CompoundCustomTriggerRange.tsx?raw";

# Custom Trigger

Replace the default trigger button with your own element using the `Trigger` render prop. When you pass a function as a child to `DatePicker.Trigger`, you receive state and callbacks to build any trigger UI you want.

<CodeDemo source={CompoundCustomTriggerSource}>
  <ExampleWrapper variant="tailwind4" client:load>
    <CompoundCustomTrigger client:load />
  </ExampleWrapper>
</CodeDemo>

## Trigger Render Props

| Prop           | Type             | Description                  |
| -------------- | ---------------- | ---------------------------- |
| `displayValue` | `string`         | Formatted selected date      |
| `placeholder`  | `string`         | Placeholder text             |
| `hasValue`     | `boolean`        | Whether a date is selected   |
| `isOpen`       | `boolean`        | Whether the popup is open    |
| `onToggle`     | `() => void`     | Toggle popup open/close      |
| `onClear`      | `() => void`     | Clear the selection          |
| `locale`       | `Locale`         | Current locale object        |
| `triggerRef`   | `(node) => void` | Ref callback for positioning |

## Range Picker Trigger

The same pattern works with `DateRangePicker`:

<CodeDemo source={CompoundCustomTriggerRangeSource}>
  <ExampleWrapper variant="tailwind4" client:load>
    <CompoundCustomTriggerRange client:load />
  </ExampleWrapper>
</CodeDemo>
