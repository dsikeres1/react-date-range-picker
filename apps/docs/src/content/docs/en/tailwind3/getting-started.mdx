---
sidebar_position: 1
sidebar_label: Getting Started
---

import CodeDemo from "@/components/CodeDemo.astro";
import PackageInstall from "@/components/PackageInstall.astro";
import { ExampleWrapper } from "@/components/ExampleWrapper";

import Tailwind3Basic from "@examples/tailwind3/DatePicker/Basic";
import Tailwind3BasicSource from "@examples/tailwind3/DatePicker/Basic.tsx?raw";

# Getting Started with Tailwind CSS v3

The Tailwind v3 package provides date picker components styled with Tailwind CSS v3 utility classes.

## Installation

<PackageInstall pkg="react-date-range-picker-tailwind3" />

No additional CSS import is needed. The components use Tailwind utility classes directly.

## Tailwind Configuration

Add the package to your `content` paths in `tailwind.config.js` so Tailwind can scan the component classes:

```js
// tailwind.config.js
module.exports = {
  content: [
    "./src/**/*.{js,ts,jsx,tsx}",
    "./node_modules/react-date-range-picker-tailwind3/dist/**/*.{js,mjs}",
  ],
  // ...
};
```

## Quick Start

<CodeDemo source={Tailwind3BasicSource}>
  <ExampleWrapper variant="tailwind3" client:load>
    <Tailwind3Basic client:load />
  </ExampleWrapper>
</CodeDemo>

## What's Included

| Component             | Description                                         |
| --------------------- | --------------------------------------------------- |
| `DatePicker`          | Single date selection with popup calendar           |
| `DateRangePicker`     | Date range selection with dual calendar and presets |
| `DateTimePicker`      | Date + time selection with time scroll panel        |
| `DateRangeTimePicker` | Date range + time selection                         |

All components support:

- Controlled value with `value` / `onChange`
- Keyboard navigation
- Dark mode via `dark:` variant
- 4 sizes: `small`, `medium`, `large`, `x-large`
- Compound Component API for customization

## TypeScript Types

All configuration and data types are re-exported from the package — no need to install the headless package separately:

```tsx
import type {
  Locale,
  DatePickerSize,
  TimeConfig,
  MinuteStep,
  SecondStep,
  TimePrecision,
  HourFormat,
  TimePeriod,
  CaptionLayout,
  WeekDay,
  DateRangePreset,
  CalendarMonth,
  DayProps,
} from "react-date-range-picker-tailwind3";
```

These types are useful for:

- **`Locale`** — Custom locale overrides (weekday names, button labels, date formatting)
- **`DatePickerSize`** — Typing the `size` prop (`"small"` | `"medium"` | `"large"` | `"x-large"`)
- **`TimeConfig`** — Configuring time picker precision, format, and steps
- **`DateRangePreset`** — Defining presets for `DateRangePicker` / `DateRangeTimePicker`
- **`CalendarMonth`**, **`DayProps`** — Custom rendering with Compound Components

See [Headless Types](/docs/headless/types/common-types) for detailed type definitions.

## Next Steps

- [DatePicker](/docs/tailwind3/components/date-picker/basic) — Single date selection
- [DateRangePicker](/docs/tailwind3/components/date-range-picker/basic) — Date range with presets
- [Compound Components](/docs/tailwind3/customization/overview) — Customize internal structure
- [Theme Override](/docs/tailwind3/customization/theme-override) — Customize theme colors
- [Dark Mode](/docs/tailwind3/customization/dark-mode) — Dark theme support

## Tailwind Preflight & Reset

This package assumes your project has Tailwind CSS Preflight enabled (which is the default).
If your environment disables Preflight (e.g., Docusaurus, some Micro Frontends), the button styles may break.
In that case, import the provided `reset.css` to manually initialize the button styles:

```tsx
import "react-date-range-picker-tailwind3/reset.css";
```
