---
sidebar_position: 2
sidebar_label: Constraints
---

import CodeDemo from "@/components/CodeDemo.astro";
import { ExampleWrapper } from "@/components/ExampleWrapper";

import Required from "@examples/styled/DatePicker/Required";
import RequiredSource from "@examples/styled/DatePicker/Required.tsx?raw";
import MinMaxDate from "@examples/styled/DatePicker/MinMaxDate";
import MinMaxDateSource from "@examples/styled/DatePicker/MinMaxDate.tsx?raw";
import DisabledDates from "@examples/styled/DatePicker/DisabledDates";
import DisabledDatesSource from "@examples/styled/DatePicker/DisabledDates.tsx?raw";
import DisablePastFuture from "@examples/styled/DatePicker/DisablePastFuture";
import DisablePastFutureSource from "@examples/styled/DatePicker/DisablePastFuture.tsx?raw";
import HighlightDates from "@examples/styled/DatePicker/HighlightDates";
import HighlightDatesSource from "@examples/styled/DatePicker/HighlightDates.tsx?raw";
import InitialMonth from "@examples/styled/DatePicker/InitialMonth";
import InitialMonthSource from "@examples/styled/DatePicker/InitialMonth.tsx?raw";

# Kısıtlamalar

## Gerekli

`required` ayarlandığında, temizle düğmesi devre dışı bırakılır ve değer boşaltılamaz.

<CodeDemo source={RequiredSource}>
  <ExampleWrapper variant="styled" client:load>
    <Required client:load />
  </ExampleWrapper>
</CodeDemo>

## Minimum ve Maksimum Tarihler

Seçilebilir tarihleri `minDate` ve `maxDate` kullanarak belirli bir aralıkla sınırlayın.

<CodeDemo source={MinMaxDateSource}>
  <ExampleWrapper variant="styled" client:load>
    <MinMaxDate client:load />
  </ExampleWrapper>
</CodeDemo>

## Devre Dışı Bırakılmış Tarihler

`isDateUnavailable` kullanarak belirli tarihleri dinamik olarak devre dışı bırakın.

<CodeDemo source={DisabledDatesSource}>
  <ExampleWrapper variant="styled" client:load>
    <DisabledDates client:load />
  </ExampleWrapper>
</CodeDemo>

## Geçmiş / Gelecek Tarihleri Devre Dışı Bırakma

Bugünden önceki/sonraki tarihleri hızla devre dışı bırakmak için `disablePast` veya `disableFuture` kullanın.

<CodeDemo source={DisablePastFutureSource}>
  <ExampleWrapper variant="styled" client:load>
    <DisablePastFuture client:load />
  </ExampleWrapper>
</CodeDemo>

## Tarihleri Vurgulama

`highlightDates` prop'unu kullanarak belirli tarihleri (ör. tatiller, etkinlikler) görsel olarak vurgulayın.

<CodeDemo source={HighlightDatesSource}>
  <ExampleWrapper variant="styled" client:load>
    <HighlightDates client:load />
  </ExampleWrapper>
</CodeDemo>

## Başlangıç Ayı

Seçici ilk kez açıldığında görüntülenecek başlangıç ayını ayarlayın.

<CodeDemo source={InitialMonthSource}>
  <ExampleWrapper variant="styled" client:load>
    <InitialMonth client:load />
  </ExampleWrapper>
</CodeDemo>
