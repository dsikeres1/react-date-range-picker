---
import type { SidebarItem } from "@/lib/sidebar";
import SidebarNav from "./SidebarNav.astro";

interface Props {
  items: SidebarItem[];
  section?: string;
}

const { items, section } = Astro.props;
const base = import.meta.env.BASE_URL;
const currentPath = Astro.url.pathname;
const version: string = __RDRP_VERSION__;

const sectionNpmMap: Record<string, string> = {
  tailwind4: "react-date-range-picker-tailwind4",
  tailwind3: "react-date-range-picker-tailwind3",
  styled: "react-date-range-picker-styled",
  headless: "react-date-range-picker-headless",
};
const npmPkg = section ? sectionNpmMap[section] : undefined;
const npmUrl = npmPkg ? `https://www.npmjs.com/package/${npmPkg}` : undefined;

const sectionLinks = [
  { label: "Tailwind v4", href: `${base}/docs/tailwind4/getting-started` },
  { label: "Tailwind v3", href: `${base}/docs/tailwind3/getting-started` },
  { label: "Styled", href: `${base}/docs/styled/getting-started` },
  { label: "Headless", href: `${base}/docs/headless/getting-started` },
  { label: "Guides", href: `${base}/docs/guides/accessibility` },
];
---

<!-- Mobile sidebar backdrop + drawer -->
<div id="sidebar-backdrop" class="fixed inset-0 z-40 hidden bg-black/50 lg:hidden"></div>
<aside
  id="sidebar-mobile"
  class="fixed inset-y-0 left-0 z-50 w-72 -translate-x-full overflow-y-auto border-r border-sidebar-border bg-sidebar px-3 py-4 transition-transform duration-200 lg:hidden"
>
  <div class="mb-4 flex items-center justify-between px-2">
    <div class="flex items-center gap-1.5">
      <span class="text-sm font-semibold text-foreground">Navigation</span>
      {
        npmUrl ? (
          <a
            href={npmUrl}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center gap-1.5 rounded-full border border-border bg-accent/50 px-2.5 py-0.5 text-xs font-medium text-muted-foreground transition-colors hover:border-primary/40 hover:text-foreground"
          >
            <svg class="size-3.5 text-[#CB3837]" viewBox="0 0 24 24" fill="currentColor">
              <path d="M0 0v24h24V0H0zm6.168 20.4H3.6V7.2h2.568v13.2zm7.2 0H10.8V7.2h5.136v10.632h-2.568V20.4zm7.2 0h-2.568V7.2H20.4v13.2z" />
            </svg>
            v{version}
            <svg
              class="size-2.5 text-muted-foreground/50"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2.5"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6" />
              <polyline points="15 3 21 3 21 9" />
              <line x1="10" y1="14" x2="21" y2="3" />
            </svg>
          </a>
        ) : (
          <span class="rounded-full bg-accent px-2.5 py-0.5 text-xs font-medium text-muted-foreground">
            v{version}
          </span>
        )
      }
    </div>
    <button
      id="sidebar-close-btn"
      type="button"
      class="rounded-md p-1.5 text-muted-foreground hover:bg-accent"
      aria-label="Close sidebar"
    >
      <svg class="size-5" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" d="M6 18 18 6M6 6l12 12"></path>
      </svg>
    </button>
  </div>
  <!-- Section links (mobile) -->
  <nav class="mb-3 border-b border-sidebar-border pb-3">
    <div class="mb-1.5 px-2 text-xs font-medium tracking-wide text-muted-foreground/60">
      Sections
    </div>
    {
      sectionLinks.map(({ label, href }) => {
        const isActive = currentPath.startsWith(href);
        return (
          <a
            href={href}
            class:list={[
              "block rounded-md px-2 py-1.5 text-sm transition-colors",
              isActive
                ? "font-medium text-foreground bg-accent"
                : "text-muted-foreground hover:text-foreground hover:bg-accent/50",
            ]}
          >
            {label}
          </a>
        );
      })
    }
  </nav>
  <SidebarNav items={items} currentPath={currentPath} />
</aside>

<!-- Desktop sidebar -->
<aside
  class="sticky top-14 hidden h-[calc(100vh-3.5rem)] w-64 shrink-0 overflow-y-auto border-r border-sidebar-border bg-sidebar px-3 py-4 lg:block"
>
  <div class="mx-2 mb-3">
    {
      npmUrl ? (
        <a
          href={npmUrl}
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center gap-1.5 rounded-full border border-border bg-accent/50 px-2.5 py-1 text-xs font-medium text-muted-foreground transition-colors hover:border-primary/40 hover:text-foreground"
        >
          <svg class="size-3.5 text-[#CB3837]" viewBox="0 0 24 24" fill="currentColor">
            <path d="M0 0v24h24V0H0zm6.168 20.4H3.6V7.2h2.568v13.2zm7.2 0H10.8V7.2h5.136v10.632h-2.568V20.4zm7.2 0h-2.568V7.2H20.4v13.2z" />
          </svg>
          v{version}
          <svg
            class="size-2.5 text-muted-foreground/50"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2.5"
            stroke-linecap="round"
            stroke-linejoin="round"
          >
            <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6" />
            <polyline points="15 3 21 3 21 9" />
            <line x1="10" y1="14" x2="21" y2="3" />
          </svg>
        </a>
      ) : (
        <span class="inline-flex rounded-full bg-accent px-2.5 py-1 text-xs font-medium text-muted-foreground">
          v{version}
        </span>
      )
    }
  </div>
  <SidebarNav items={items} currentPath={currentPath} />
</aside>

<script is:inline>
  // === Mobile sidebar toggle ===
  const backdrop = document.getElementById("sidebar-backdrop");
  const sidebar = document.getElementById("sidebar-mobile");
  const closeBtn = document.getElementById("sidebar-close-btn");

  function openSidebar() {
    backdrop?.classList.remove("hidden");
    sidebar?.classList.remove("-translate-x-full");
  }

  function closeSidebar() {
    backdrop?.classList.add("hidden");
    sidebar?.classList.add("-translate-x-full");
  }

  document.getElementById("mobile-menu-btn")?.addEventListener("click", (e) => {
    e.preventDefault();
    e.stopPropagation();
    document.getElementById("mobile-menu")?.classList.add("hidden");
    openSidebar();
  });

  closeBtn?.addEventListener("click", closeSidebar);
  backdrop?.addEventListener("click", closeSidebar);

  // === Accordion behavior ===
  // Strategy: animate with max-height for open/close, then switch to
  // max-height:none when expanded so nested children can grow freely.
  document.querySelectorAll("[data-sidebar-toggle]").forEach((btn) => {
    const group = btn.closest("[data-sidebar-group]");
    const content = group?.querySelector(":scope > [data-sidebar-content]");
    const chevron = btn.querySelector("[data-sidebar-chevron]");
    if (!content) return;

    // Initialize from SSR data-expanded attribute
    const isExpanded = btn.getAttribute("data-expanded") === "true";
    if (isExpanded) {
      content.style.maxHeight = "none";
      chevron?.classList.add("rotate-90");
    } else {
      content.style.maxHeight = "0";
    }

    btn.addEventListener("click", () => {
      const expanded = btn.getAttribute("aria-expanded") === "true";
      const next = !expanded;

      btn.setAttribute("aria-expanded", String(next));
      btn.setAttribute("data-expanded", String(next));
      chevron?.classList.toggle("rotate-90", next);

      if (next) {
        // Expand: 0 -> scrollHeight (animated) -> none (after transition)
        content.style.maxHeight = content.scrollHeight + "px";
        content.addEventListener(
          "transitionend",
          () => {
            if (btn.getAttribute("data-expanded") === "true") {
              content.style.maxHeight = "none";
            }
          },
          { once: true },
        );
      } else {
        // Collapse: none -> scrollHeight (instant) -> 0 (animated)
        content.style.maxHeight = content.scrollHeight + "px";
        content.offsetHeight; // force reflow
        content.style.maxHeight = "0";
      }
    });
  });
</script>
