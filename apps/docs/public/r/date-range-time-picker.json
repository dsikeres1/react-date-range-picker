{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "date-range-time-picker",
  "title": "Date Range Time Picker",
  "description": "A dual-calendar date range picker with start/end time selection and presets.",
  "dependencies": [
    "react-date-range-picker-headless",
    "@floating-ui/react",
    "clsx",
    "tailwind-merge"
  ],
  "registryDependencies": [
    "https://dsikeres1.github.io/react-date-range-picker/r/date-range-picker.json"
  ],
  "files": [
    {
      "path": "registry/date-range-time-picker.tsx",
      "content": "import { useEffect, useRef } from \"react\";\nimport { useDateRangeTimePicker, formatBasic } from \"react-date-range-picker-headless\";\nimport { useFloating, autoUpdate, offset, flip, shift, FloatingPortal } from \"@floating-ui/react\";\nimport type { DateRangeTimePickerProps } from \"@/lib/types\";\nimport { cn, sizeClass } from \"@/lib/utils\";\nimport { mergeRefs } from \"@/lib/merge-refs\";\nimport { TimePanelInternal } from \"@/lib/time-panel-internal\";\nimport { CalendarGrid } from \"./calendar-grid\";\n\nexport function DateRangeTimePicker({\n  className,\n  style,\n  inline,\n  hideHeader,\n  hideFooter,\n  hideWeekdays,\n  renderTrigger,\n  renderHeader,\n  renderWeekdayRow,\n  renderDayCell,\n  renderFooter,\n  renderTimeSection,\n  renderPresetsBar,\n  ...options\n}: DateRangeTimePickerProps) {\n  const picker = useDateRangeTimePicker(options);\n  const { locale, isOpen, resolvedTimeConfig } = picker;\n  const showContent = inline || isOpen;\n  const hasPresets = picker.presets.length > 0;\n  const localPopupRef = useRef<HTMLDivElement>(null);\n\n  useEffect(() => {\n    if (isOpen && !inline && localPopupRef.current) {\n      localPopupRef.current.focus({ preventScroll: true });\n    }\n  }, [isOpen, inline]);\n\n  const { refs, floatingStyles } = useFloating({\n    open: !inline && isOpen,\n    placement: \"bottom-start\",\n    middleware: [offset(8), flip(), shift({ padding: 8 })],\n    whileElementsMounted: autoUpdate,\n  });\n\n  const triggerEl =\n    !inline &&\n    (renderTrigger ? (\n      renderTrigger({\n        displayValue: picker.displayValue,\n        placeholder: locale.rangeTimePlaceholder,\n        hasValue: picker.hasValue,\n        isOpen,\n        onToggle: picker.handleToggle,\n        onClear: picker.handleClear,\n        locale,\n        triggerRef: refs.setReference as (node: HTMLElement | null) => void,\n      })\n    ) : (\n      <button\n        type=\"button\"\n        className=\"flex items-center justify-between min-w-[200px] px-3.5 py-2 bg-background border border-input rounded-md shadow-sm hover:border-ring/50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:border-ring\"\n        onClick={picker.handleToggle}\n        aria-expanded={isOpen}\n        aria-haspopup=\"dialog\"\n      >\n        <span className={picker.displayValue ? undefined : \"text-muted-foreground\"}>\n          {picker.displayValue || locale.rangeTimePlaceholder}\n        </span>\n        {picker.hasValue && !options.required && (\n          <button\n            type=\"button\"\n            className=\"ml-2 p-1 -m-1 appearance-none border-0 bg-transparent text-muted-foreground hover:text-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring rounded-sm cursor-pointer\"\n            aria-label={locale.clear}\n            onClick={(e) => {\n              e.stopPropagation();\n              picker.handleClear();\n            }}\n          >\n            \u00d7\n          </button>\n        )}\n      </button>\n    ));\n\n  const headerEl =\n    !hideHeader &&\n    showContent &&\n    (renderHeader ? (\n      renderHeader({\n        month: picker.leftMonth,\n        formattedMonthYear: locale.formatMonthYear(picker.leftMonth),\n        onPrevMonth: picker.handlePrevMonth,\n        onNextMonth: picker.handleNextMonth,\n        locale,\n        captionLayout: options.captionLayout ?? \"buttons\",\n        years: picker.years,\n        months: picker.months,\n        onYearSelect: picker.handleYearSelect,\n        onMonthSelect: picker.handleMonthSelect,\n      })\n    ) : (\n      <div className=\"flex items-center justify-between mb-4\">\n        <button\n          type=\"button\"\n          className=\"flex items-center justify-center w-7 h-7 text-muted-foreground rounded hover:bg-accent hover:text-accent-foreground cursor-pointer\"\n          onClick={picker.handlePrevMonth}\n        >\n          {locale.prevMonth}\n        </button>\n        <div className=\"flex-1\" />\n        <button\n          type=\"button\"\n          className=\"flex items-center justify-center w-7 h-7 text-muted-foreground rounded hover:bg-accent hover:text-accent-foreground cursor-pointer\"\n          onClick={picker.handleNextMonth}\n        >\n          {locale.nextMonth}\n        </button>\n      </div>\n    ));\n\n  const presetsEl =\n    hasPresets &&\n    showContent &&\n    (renderPresetsBar ? (\n      renderPresetsBar({\n        presets: picker.presets,\n        activePresetIndex: picker.activePresetIndex,\n        onPresetClick: picker.handlePresetClick,\n      })\n    ) : (\n      <div className=\"flex flex-col gap-1 pr-4 border-r border-border min-w-[140px]\">\n        {picker.presets.map((preset, i) => (\n          <button\n            type=\"button\"\n            key={preset.label}\n            className={cn(\n              \"px-3 py-1.5 text-sm text-left rounded-md transition-colors hover:bg-accent cursor-pointer\",\n              i === picker.activePresetIndex && \"bg-primary/10 text-primary font-medium\",\n            )}\n            onClick={() => picker.handlePresetClick(preset)}\n          >\n            {preset.label}\n          </button>\n        ))}\n      </div>\n    ));\n\n  const timeSectionEl =\n    showContent &&\n    (renderTimeSection ? (\n      renderTimeSection({\n        timeConfig: resolvedTimeConfig,\n        start: {\n          displayValue: picker.startTimeDisplayValue,\n          label: locale.startTimeLabel || \"Start\",\n          hour: picker.startHour,\n          minute: picker.startMinute,\n          second: picker.startSecond,\n          period: picker.startPeriod,\n        },\n        end: {\n          displayValue: picker.endTimeDisplayValue,\n          label: locale.endTimeLabel || \"End\",\n          hour: picker.endHour,\n          minute: picker.endMinute,\n          second: picker.endSecond,\n          period: picker.endPeriod,\n        },\n      })\n    ) : (\n      <div className=\"flex gap-6 mt-4 pt-4 border-t border-border\">\n        <div className=\"flex flex-col gap-1\">\n          <span className=\"text-xs font-medium text-muted-foreground\">\n            {locale.startTimeLabel || \"Start\"}\n          </span>\n          <TimePanelInternal\n            hour={picker.startHour}\n            minute={picker.startMinute}\n            second={picker.startSecond}\n            period={picker.startPeriod}\n            onHourChange={picker.handleStartHourChange}\n            onMinuteChange={picker.handleStartMinuteChange}\n            onSecondChange={picker.handleStartSecondChange}\n            onPeriodChange={picker.handleStartPeriodChange}\n            timeConfig={resolvedTimeConfig}\n          />\n        </div>\n        <div className=\"flex flex-col gap-1\">\n          <span className=\"text-xs font-medium text-muted-foreground\">\n            {locale.endTimeLabel || \"End\"}\n          </span>\n          <TimePanelInternal\n            hour={picker.endHour}\n            minute={picker.endMinute}\n            second={picker.endSecond}\n            period={picker.endPeriod}\n            onHourChange={picker.handleEndHourChange}\n            onMinuteChange={picker.handleEndMinuteChange}\n            onSecondChange={picker.handleEndSecondChange}\n            onPeriodChange={picker.handleEndPeriodChange}\n            timeConfig={resolvedTimeConfig}\n          />\n        </div>\n      </div>\n    ));\n\n  const footerEl =\n    !hideFooter &&\n    showContent &&\n    (renderFooter ? (\n      renderFooter({\n        canConfirm: picker.canConfirm,\n        hasValue: picker.hasValue,\n        onConfirm: picker.handleConfirm,\n        onCancel: picker.handleCancel,\n        onClear: picker.handleClear,\n        locale,\n      })\n    ) : (\n      <div className=\"flex justify-end gap-2 mt-4 pt-4 border-t border-border\">\n        {picker.hasValue && (\n          <button\n            type=\"button\"\n            className=\"px-3 py-1.5 text-sm text-muted-foreground hover:text-foreground hover:bg-accent rounded-md transition-colors cursor-pointer\"\n            onClick={picker.handleClear}\n          >\n            {locale.clear}\n          </button>\n        )}\n        <button\n          type=\"button\"\n          className=\"px-3 py-1.5 text-sm text-muted-foreground hover:text-foreground hover:bg-accent rounded-md transition-colors cursor-pointer\"\n          onClick={picker.handleCancel}\n        >\n          {locale.cancel}\n        </button>\n        <button\n          type=\"button\"\n          className=\"px-3 py-1.5 text-sm text-primary-foreground bg-primary hover:bg-primary/90 rounded-md transition-colors disabled:opacity-50 disabled:pointer-events-none cursor-pointer\"\n          onClick={picker.handleConfirm}\n          disabled={!picker.canConfirm}\n        >\n          {locale.confirm}\n        </button>\n      </div>\n    ));\n\n  const calendarsEl = (\n    <div className=\"flex flex-wrap gap-8 max-sm:flex-col max-sm:gap-4\">\n      {picker.calendars.map((cal, i) => (\n        <CalendarGrid\n          key={formatBasic(cal.month, \"YYYY-MM\")}\n          calendar={cal}\n          locale={locale}\n          focusedDate={picker.focusedDate}\n          getDayProps={picker.getDayProps}\n          onDateClick={picker.handleDateClick}\n          onDateHover={picker.handleDateHover}\n          showTitle\n          hideWeekdays={hideWeekdays}\n          captionLayout={options.captionLayout}\n          years={picker.years}\n          monthOptions={picker.months}\n          onYearSelect={picker.handleYearSelect}\n          onMonthSelect={picker.handleMonthSelect}\n          calendarIndex={i}\n          renderWeekdayRow={renderWeekdayRow}\n          renderDayCell={renderDayCell}\n        />\n      ))}\n    </div>\n  );\n\n  const popupContent = (\n    <div className={cn(hasPresets && \"flex gap-4\")}>\n      {presetsEl}\n      <div>\n        {headerEl}\n        {calendarsEl}\n        {timeSectionEl}\n        {footerEl}\n      </div>\n    </div>\n  );\n\n  return (\n    <div\n      ref={mergeRefs(picker.containerRef, refs.setReference)}\n      className={cn(\n        \"relative w-max font-sans text-[13px] text-foreground select-none\",\n        sizeClass(options.size),\n        className,\n      )}\n      style={style}\n    >\n      {triggerEl}\n\n      {showContent && !inline && (\n        <FloatingPortal>\n          <div\n            ref={mergeRefs(refs.setFloating, picker.popupRef, localPopupRef)}\n            style={floatingStyles}\n            className=\"z-50 p-4 bg-popover border border-border rounded-lg shadow-md\"\n            onKeyDown={picker.handleKeyDown}\n            tabIndex={0}\n            role=\"dialog\"\n            aria-modal=\"true\"\n            aria-label={locale.rangeTimePlaceholder}\n            aria-activedescendant={\n              picker.focusedDate ? `rdrp-day-${locale.formatDate(picker.focusedDate)}` : undefined\n            }\n          >\n            {popupContent}\n          </div>\n        </FloatingPortal>\n      )}\n\n      {showContent && inline && (\n        <div\n          className=\"p-4 bg-popover border border-border rounded-lg\"\n          onKeyDown={picker.handleKeyDown}\n          tabIndex={0}\n          role=\"group\"\n          aria-label=\"Date range and time picker\"\n          aria-activedescendant={\n            picker.focusedDate ? `rdrp-day-${locale.formatDate(picker.focusedDate)}` : undefined\n          }\n        >\n          {popupContent}\n        </div>\n      )}\n    </div>\n  );\n}\n",
      "type": "registry:component"
    },
    {
      "path": "registry/lib/time-panel-internal.tsx",
      "content": "import type { RefObject } from \"react\";\nimport { useTimePicker, padNumber, to12Hour } from \"react-date-range-picker-headless\";\nimport type { TimePanelInternalProps } from \"./types\";\nimport { cn } from \"./utils\";\n\nexport function TimePanelInternal({\n  hour,\n  minute,\n  second,\n  period,\n  onHourChange,\n  onMinuteChange,\n  onSecondChange,\n  onPeriodChange,\n  timeConfig,\n}: TimePanelInternalProps) {\n  const displayHour = timeConfig.hourFormat === \"12\" ? to12Hour(hour).hour : hour;\n\n  const tp = useTimePicker({\n    hour: displayHour,\n    minute,\n    second,\n    period,\n    onHourChange,\n    onMinuteChange,\n    onSecondChange,\n    onPeriodChange,\n    precision: timeConfig.precision,\n    hourFormat: timeConfig.hourFormat,\n    minuteStep: timeConfig.minuteStep,\n    secondStep: timeConfig.secondStep,\n    itemHeight: timeConfig.itemHeight,\n  });\n\n  const handleKeyDown = (\n    e: React.KeyboardEvent,\n    items: number[],\n    selectedValue: number,\n    onItemClick: (v: number) => void,\n  ) => {\n    const currentIndex = items.indexOf(selectedValue);\n    if (currentIndex < 0) return;\n\n    const findNext = (from: number, dir: 1 | -1): number => {\n      let idx = from;\n      do {\n        idx = dir === 1\n          ? (idx < items.length - 1 ? idx + 1 : 0)\n          : (idx > 0 ? idx - 1 : items.length - 1);\n      } while (items[idx] === -1 && idx !== from);\n      return idx;\n    };\n\n    if (e.key === \"ArrowUp\") {\n      e.preventDefault();\n      const next = findNext(currentIndex, -1);\n      if (items[next] !== -1) onItemClick(items[next]);\n    } else if (e.key === \"ArrowDown\") {\n      e.preventDefault();\n      const next = findNext(currentIndex, 1);\n      if (items[next] !== -1) onItemClick(items[next]);\n    } else if (e.key === \"Home\") {\n      e.preventDefault();\n      const first = items.findIndex((v) => v !== -1);\n      if (first >= 0) onItemClick(items[first]);\n    } else if (e.key === \"End\") {\n      e.preventDefault();\n      for (let i = items.length - 1; i >= 0; i--) {\n        if (items[i] !== -1) { onItemClick(items[i]); break; }\n      }\n    } else if (e.key === \"Enter\" || e.key === \" \") {\n      e.preventDefault();\n    }\n  };\n\n  const renderColumn = (\n    ref: RefObject<HTMLDivElement | null>,\n    items: number[],\n    selectedValue: number,\n    onItemClick: (v: number) => void,\n    onScroll: () => void,\n    slug: string,\n    ariaLabel?: string,\n  ) => (\n    <div\n      ref={ref}\n      className=\"flex-1 h-[160px] overflow-y-auto [scrollbar-width:none] [scroll-snap-type:y_mandatory] [&::-webkit-scrollbar]:hidden\"\n      onScroll={onScroll}\n      role=\"listbox\"\n      aria-label={ariaLabel}\n      tabIndex={0}\n      aria-activedescendant={`time-${slug}-${selectedValue}`}\n      onKeyDown={(e) => handleKeyDown(e, items, selectedValue, onItemClick)}\n    >\n      {items.map((item, i) =>\n        item === -1 ? (\n          <div key={`pad-${slug}-${i}`} className=\"h-8 shrink-0\" aria-hidden=\"true\" />\n        ) : (\n          <div\n            id={`time-${slug}-${item}`}\n            key={`${slug}-${item}`}\n            role=\"option\"\n            aria-selected={item === selectedValue}\n            aria-label={ariaLabel ? `${padNumber(item)} ${ariaLabel.toLowerCase()}` : undefined}\n            data-slot=\"time-item\"\n            className={cn(\n              \"h-8 flex items-center justify-center cursor-pointer text-sm text-muted-foreground [scroll-snap-align:center] transition-colors select-none hover:text-foreground\",\n              item === selectedValue && \"text-foreground font-semibold\",\n            )}\n            onClick={() => onItemClick(item)}\n          >\n            {padNumber(item)}\n          </div>\n        ),\n      )}\n    </div>\n  );\n\n  return (\n    <div\n      className=\"relative inline-flex items-center justify-center min-w-[120px] h-[176px]\"\n      data-slot=\"time-panel\"\n    >\n      <div className=\"absolute left-1 right-1 h-8 rounded-md bg-primary/8 pointer-events-none\" />\n      <div className=\"relative flex items-center w-full h-full\">\n        {renderColumn(\n          tp.hourListRef,\n          tp.hours,\n          displayHour,\n          tp.handleHourClick,\n          tp.handleHourScroll,\n          \"hour\",\n          \"Hours\",\n        )}\n\n        {tp.showMinutes && (\n          <>\n            <div className=\"text-sm font-semibold text-foreground w-3 text-center shrink-0\">:</div>\n            {renderColumn(\n              tp.minuteListRef,\n              tp.minutes,\n              minute,\n              tp.handleMinuteClick,\n              tp.handleMinuteScroll,\n              \"minute\",\n              \"Minutes\",\n            )}\n          </>\n        )}\n\n        {tp.showSeconds && (\n          <>\n            <div className=\"text-sm font-semibold text-foreground w-3 text-center shrink-0\">:</div>\n            {renderColumn(\n              tp.secondListRef,\n              tp.seconds,\n              second,\n              tp.handleSecondClick,\n              tp.handleSecondScroll,\n              \"second\",\n              \"Seconds\",\n            )}\n          </>\n        )}\n\n        {tp.is12Hour && (\n          <button\n            type=\"button\"\n            className=\"h-8 flex items-center justify-center cursor-pointer text-sm text-foreground font-semibold hover:bg-accent hover:text-accent-foreground rounded-md\"\n            onClick={tp.handlePeriodToggle}\n            aria-label={`Toggle AM/PM, current: ${period}`}\n            aria-pressed={period === \"PM\"}\n          >\n            {period}\n          </button>\n        )}\n      </div>\n    </div>\n  );\n}\n\n",
      "type": "registry:lib"
    }
  ],
  "type": "registry:component"
}
